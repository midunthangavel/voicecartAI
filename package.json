{
  "name": "voicecart-ai-root",
  "version": "2.0.0",
  "description": "VoiceCart AI — Bilingual Voice Telephony & ONDC Food Ordering System",
  "private": true,
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "dev": "node -e \"const { spawn } = require('child_process'); spawn('npm', ['--prefix', 'server', 'run', 'dev'], { stdio: 'inherit', shell: true }); spawn('npm', ['--prefix', 'client', 'run', 'dev'], { stdio: 'inherit', shell: true });\"",
    "start": "npm --prefix server start",
    "mobile": "npm --prefix mobile start",
    "mobile:tunnel": "npm --prefix mobile start -- --tunnel",
    "test": "npm --prefix server test",
    "build": "npm --prefix client run build",
    "sec:audit": "node security-suite/runner.js --target all",
    "sec:server": "node security-suite/runner.js --target server",
    "sec:client": "node security-suite/runner.js --target client",
    "sec:mobile": "node security-suite/runner.js --target mobile",
    "sec:loop": "node security-suite/runner.js --target all --loop",
    "sec:strix": "node security-suite/runner.js --strix"
  }
}
